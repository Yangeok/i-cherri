# TODO

- Build a macOS failed-upload history screen backed by `upload_failure_logs` so users can inspect terminal backup failures without mixing them into active sessions.
- Add filtering and retry affordances for failed-upload history once the screen exists.
